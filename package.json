{
  "name": "trail-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Hiking activity dashboard",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step'"
  }
}
